
schemer_import('darksteel.prf', true);

for i= 1:16
    if i < 10
       dataBase{i}=imread(['db1_0' int2str(i) '.jpg']); 
    end
    if i >=10
       dataBase{i}=imread(['db1_' int2str(i) '.jpg']); 
    end
end
for i = 1:15
    im = imfuse(dataBase{i}, dataBase{i+1}, 'blend');
end
%im = imread('db1_12.jpg');
imshow(im)

