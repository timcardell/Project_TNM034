
schemer_import('darksteel.prf', true);
for i = 1:16
    
    if i < 10
   imres = (imread(['db4_0' int2str(i) '.jpg'])); 
end
 
if i >=10
   imres = (imread(['db4_' int2str(i) '.jpg'])); 
end
    
    im = imread('db1_01.jpg');
% imshow(im)
 figure
ID(i) = tnm034(im);
end


if ID < 10
   imres = (imread(['db4_0' int2str(ID) '.jpg'])); 
end
 
if ID >=10
   imres = (imread(['db4_' int2str(ID) '.jpg'])); 
end

% imshow(imres)
